# Deploy configuration
# Copy to deploy.env and fill in your values

SERVER_HOST=185.195.27.99
SERVER_USER=root
PZ_USER=pzserver

# Local paths
MOD_DIR="$HOME/projects/planb-server/mods/SafeZone"
SCRIPTS_DIR="$HOME/projects/planb-server/server-scripts"
CONFIG_DIR="$HOME/projects/planb-server/config"

# Remote paths
REMOTE_UPLOAD_DIR=/home/pzserver/workshop-upload
REMOTE_LUA_DIR=/home/pzserver/Zomboid/Lua
REMOTE_SERVER_DIR=/home/pzserver/Zomboid/Server

# Steam
STEAM_USER=vortololo
STEAMCMD_PATH=/home/pzserver/steamcmd/steamcmd.sh